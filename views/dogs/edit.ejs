<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Describe a dog</title>
</head>
<body>
    <h1>Describe a dog!!</h1>

    <form action="/dogs/<%=dog._id%>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" value="<%= dog.name %>" />

        <label for="breed">breed:</label>
        <input type="text" name="breed" value="<%= dog.breed %>" />

            <label for="age">age:</label>
            <input type="number" name="age" value="<%= dog.age %>" />

    <label for="isCute">Is the dog cute?</label>
    <input type="checkbox" name="isCute" <% if (dog.isCute) { %>checked<% } %> />

    <button type="submit">Update Dog</button>
</form>
    <a href="/dogs/">Back to dogs</a>

</body>
</html>